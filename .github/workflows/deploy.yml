name: Deploy

on:
  push:
    branches: [main, dev]

jobs:
  deploy:
    uses: AtlasReaper311/atlas-infra/.github/workflows/deploy-worker.yml@main
    with:
      worker_name: github-pulse
      run_lint: true
    secrets: inherit
